const permutations = (str) => {
  let x = str.split('');
  const results = [];

  for (let i = 0; i <= x.length; i++) {
    let perm = x[i]
    if (!results.include(perm)) {
      results.push()
    }
  }

  slice
  splice

  return results;
}
console.log(permutations("string"))
module.exports = permutations;

//take the first item
//
//let cat = ["cat", "cta", "atc", "act", ""]
